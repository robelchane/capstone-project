/*import express from 'express';
import AuthRoute from './api/auth/signup/route';

const app = express();

app.use(express.json());

app.use('/auth', AuthRoute);

app.listen(3000, () => {
    console.log('Server is running on port 3000');
}
);
*/